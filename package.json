{
  "name": "zebra-riddle-solver",
  "version": "1.0.0",
  "description": "本專案是一個基於 HTML 與 JavaScript（ES Module）建構的互動式 **斑馬難題（Zebra Riddle）** 推理輔助工具。",
  "license": "ISC",
  "author": "ohayowu314",
  "type": "module",
  "main": "app.js",
  "directories": {
    "doc": "docs",
    "example": "example"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && copyfiles -f index.html dist && copyfiles -u 1 \"src/**/*.{json,css,png,svg}\" dist"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^7.0.2"
  }
}