# zebra riddle 一個推理題目

## 結構
N 個個體(通常是人，但也可以是房子、車子等)從左至右排成一列，左側為第一個位置，然後第二個位置等等，每個個體有 M 個特徵(例如名字、喜好、衣著顏色等)，每個特徵都有 N 個不同的可能值，且不同個體之間同一個特徵不會有相同的值。
然後給你 L 條限制條件，你經由思考推理出符合所有限制條件的唯一答案。

## 限制條件
限制條件的形式有:
- 確定位置: 特徵 i 的值為 j 的個體位於第 k 個位置 => 這是最好的條件，因為它直接說明答案
- 相鄰: 特徵 i1 的值為 j1 的個體 與 特徵 i2 的值為 j2 的個體 相鄰，但不知道順序 => 注意 特徵 i1 特徵 i2 不一定是同一個，後面類似
- 確定順序: 特徵 i1 的值為 j1 的個體 在 特徵 i2 的值為 j2 的個體 的 (左側 右側 選一種) 但不一定相鄰
- 確定順序且相鄰: 特徵 i1 的值為 j1 的個體 在 特徵 i2 的值為 j2 的個體 的 (左側 右側 選一種) 隔壁
- 三者確定順序: 特徵 i1 的值為 j1 的個體 , 特徵 i2 的值為 j2 的個體 , 特徵 i3 的值為 j3 的個體 三者按照此順序 但不一定相鄰
- 可能位置: 特徵 i 的值為 j 的個體位於 第 k1 個位置 或者 第 k2 個位置 => 這雖然不能完全確定，但也能排除很多可能
- 確定相同: 特徵 i1 的值為 j1 的個體 就是 特徵 i2 的值為 j2 的個體 => 這裡的 特徵 i1 特徵 i2 一定不同

其他還有一些更複雜的限制條件，但我們不加贅述

## 專案目標
注意，本專案並不是要直接計算出解答或者暴力解篩選出解答

> 暴力解篩選 幾乎不可能，畢竟光是 5 個個體 6 特徵就會有 (5!)^6 種可能答案

本專案是提供一個合適的視覺化介面，輔助使用者一步步推理出解答。它本身不會直接算出解答，而是通過不斷與使用者交互，顯現出一些可能答案(或其中一部分)，讓使用者在各種可能中進行推導出解答。

## 介面設計
介面分為以下幾個頁簽部分

- 個體特徵設定頁簽: 包含 個體數量、特徵數量、個體增減、特徵增減、特徵可能值增修減 輸入修改區域，用來生成整個題目基礎
- 規則選擇與設定頁簽: 包含 規則增減、規則類型選擇、規則內容設定
- 推理互動頁簽: 這部分最複雜，後面單獨開一節說明

## 推理互動頁簽
再次強調，本專案不是要直接計算出解答或者暴力解篩選出解答

此頁簽分為「解答區」、「特徵值剩餘位置區」、「特徵值可行位置區」、「規則展示與特徵可行位置區」
- 解答區: 位於畫面上方左側，主要為表格，欄位代表每一個個體位置，每一列對應一個特徵，每一個儲存格就是一個選擇區，供使用者選他認為的該個體該特徵的正確值
- 特徵值剩餘位置區: 位於畫面上方中間，主要為表格，每兩列對應一個特徵，先列出該特徵所有的值，下一列則是該值剩餘可能位置，由解答區推導出來。用 "1,3,4" 來表達可能位置
- 規則展示與特徵可行位置區: 位於畫面下方，主要為表格，由以下單元構成

  ```
  ------------------------------------------------------------------------
              | 該規則牽涉的特徵值所有可能的排列方式 | 該規則牽涉的特徵值
  規則簡要描述 |-----------------------------------------------------------
              | 該特徵值符合該排列方式的可行位置     | 該特徵值所有可行位置
  ------------------------------------------------------------------------
  ```

  - 規則簡要描述: 就是前面提到的限制條件，因為特徵的值通常不會混淆，可以簡單寫成 "穿紅衣的 在 養貓的 左側隔壁"、"玩吉他的 和 喜歡茶的 在隔壁"、"英國人, 55歲的, 郵差 按此順序"
  - 該規則牽涉的特徵值所有可能的排列方式: 將特徵值按照規則描述，配合以下符號排列組合，組出所有可能的排列方式
    - 確定順序且為隔壁的符號 `特徵值 | 特徵值` 注意此符號可以延續 如  `特徵值 | 特徵值 | 特徵值`
    - 確定順序但不一定為隔壁的符號 `特徵值 . 特徵值` 注意此符號可以延續 `特徵值 . 特徵值 . 特徵值`
    - 確定相同的符號 `特徵值 = 特徵值`
    - 確定位置 僅 `特徵值` 即可
    - 表示邏輯或的符號 `語句 v 語句`
  - 該特徵值符合該排列方式的可行位置: 根據 特徵值剩餘位置區 和上面的 特徵值排列方式 ，計算出可行位置，像是 "第一個位置左邊不可能有值"、"最後一個位置右邊不可能有值"，這一類的需要排除。用 "1,3,4" 來表達該特徵值可行位置，注意要和上方特徵值對齊
  - 該規則牽涉的特徵值: 列出該規則牽涉的特徵值
  - 該特徵值所有可行位置: 綜合所有可行位置，注意和上方的特徵值對齊。主要是要小心 邏輯或，此時需要合併可行值


- 特徵值可行位置區: 位於畫面上方右側，主要為表格，類似 特徵值剩餘位置區，但通過 規則展示與特徵可行位置區 篩選剩餘可行組合。