現在各種規則寫死在代碼裡面，且規則的內容散落在各處
包含規則要如何在第二頁簽新增、規則的代碼、規則簡要描述如何構成、規則如何組成特徵值排列方式、又該如何計算出對應之可行位置等等
如果我想加入新種類的規則，又或者修改乃至於刪除已有規則，都會很麻煩

有沒有辦法將規則部分拆出來，並設計一個規則引擎，我僅需添加一些資料夾路徑，如 `rules/rule-adjacent`，引擎自動幫我從該資料夾讀取並生成相應規則。

我的設想中每一個規則資料夾內需要有 json 或者 yaml 來描述相關資訊，諸如規則的名稱、代號、需要幾個輸入變數、各變數儲存名稱和顯示名稱以及輸入類型等等；然後一個 js 檔案，裡面儲存程式碼，包含規則簡要描述的組裝、特徵值排列方式、如何計算可行位置等等

請先不要產生代碼，而是先和我討論可行性，以及還有那些不足或是錯誤

# ======================

1. 原本的代碼是純 HTML，所以更適合哪一種?
2. 我認為規則之間不能存在相依關係，而最終解答會是各規則計算出的可行結果的交集，因為要滿足所有規則

# ======================

採用方案 A，還有這是目前的代碼，請從其中解析 meta.json 所需的結構與 index.js 各函數介面(輸入與輸出)

# ======================

所以 state.rules 更動 由 meta.json 配合 buildDescription得到

# ======================

那麼現在修改 index.html 加入規則引擎功能以及相應變動，預設規則和範例都先刪除

